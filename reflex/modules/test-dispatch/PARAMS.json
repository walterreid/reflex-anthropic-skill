{
  "group": "meta",
  "description": "Smoke test dispatch routing across all levels, features, and edge cases",
  "usage": "test reflex routing, smoke test dispatch, verify routing works",
  "params": {
    "scope": {
      "required": false,
      "description": "What to test: all, routing, params, chains, resolvers, conditionals",
      "default": "all"
    }
  }
}
