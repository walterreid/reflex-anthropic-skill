# Plan Module

Translate a natural language intent into action — either a runnable chain from existing modules, or a new module design when the registry can't serve the intent well.

- **Intent**: {intent}

The resolver has analyzed the intent against the module registry and selected the appropriate variant. Follow the variant's instructions.
