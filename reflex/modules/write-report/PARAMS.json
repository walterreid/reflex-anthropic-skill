{
  "params": {
    "topic": {
      "required": true,
      "description": "Subject of the report"
    },
    "format": {
      "required": false,
      "description": "Report format: brief, full, or memo",
      "default": "brief"
    },
    "workspace": {
      "inject": "workspace_state"
    },
    "lenses": {
      "inject": "lens_library"
    }
  },
  "group": "formatter",
  "description": "Format findings as a structured report or memo — reads workspace research artifacts, applies lens concern self-assessment, produces a traceable deliverable grounded in evidence. Use this instead of writing documents directly.",
  "usage": "write a report, create a memo, format as report, structured writeup, document as report, write something professional, put together a document, write up the findings, make a doc"
}