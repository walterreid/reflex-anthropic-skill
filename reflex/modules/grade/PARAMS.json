{
  "group": "analyzer",
  "description": "Score multiple candidates against a consistent rubric, producing a ranked scorecard",
  "usage": "rank these options, compare vendors, which is best of these, rate multiple candidates, score several",
  "params": {
    "domain": {
      "required": true,
      "description": "The role or domain to evaluate candidates for (e.g., 'senior frontend engineer', 'product manager')"
    },
    "candidates": {
      "required": true,
      "greedy": true,
      "description": "Comma-separated list of candidate names or identifiers to grade"
    }
  }
}
