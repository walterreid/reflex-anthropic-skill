{
  "group": "formatter",
  "description": "Compress analysis into a send-ready email using the interactive message compose tool — reads workspace findings, applies lens concern self-assessment, grounds every claim in evidence. Use this instead of drafting emails directly.",
  "usage": "write an email, draft a message, cold email, email to someone about, compose mail, launch email, send an email, email him, write him an email",
  "params": {
    "target": {
      "required": true,
      "description": "Subject being communicated about"
    },
    "recipient": {
      "required": false,
      "description": "Who receives this: cold-list, manager, ceo, client, team, investor, partner, customer",
      "default": "manager"
    },
    "tone": {
      "required": false,
      "description": "Email tone: formal, direct, warm, bold, conversational",
      "default": "direct"
    },
    "variants": {
      "required": false,
      "description": "Number of strategic variants to generate (1-3). Multiple variants offer different approaches, not just different tones.",
      "default": "1"
    },
    "findings": {
      "required": false,
      "description": "Upstream research or analysis findings from chained modules"
    },
    "workspace": {
      "inject": "workspace_state"
    },
    "lenses": {
      "inject": "lens_library"
    }
  }
}
