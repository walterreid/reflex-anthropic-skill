{
  "group": "meta",
  "description": "Interactive module designer — create new modules, upgrade existing ones, or clone and modify",
  "usage": "create a new reflex module, build a custom module, design a reflex module, upgrade module, edit module, improve module, change module, fix module, modify module",
  "params": {
    "intent": {
      "required": false,
      "description": "What the new module should do, or which module to modify, in natural language",
      "greedy": true,
      "default": ""
    },
    "registry": {
      "inject": "module_registry"
    },
    "workspace": {
      "inject": "workspace_state"
    }
  }
}
