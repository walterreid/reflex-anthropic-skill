{
  "group": "meta",
  "description": "Interactive module designer — walks a user through creating a new Reflex module with the right level, group, params, dependencies, and inject sources",
  "usage": "create a new reflex module, build a custom module, design a reflex module",
  "params": {
    "intent": {
      "required": true,
      "description": "What the new module should do, in natural language",
      "greedy": true
    },
    "registry": {
      "inject": "module_registry"
    },
    "workspace": {
      "inject": "workspace_state"
    }
  }
}
